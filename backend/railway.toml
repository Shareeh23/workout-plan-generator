[build]
  builder = "DOCKER"
  dockerfile = "../Dockerfile"

[deploy]
  startCommand = "npm start"

[variables]
  NODE_ENV = "production"
  PORT = 3000

[healthcheck]
  enabled = true
  interval = 30
  retries = 3
  timeout = 10
  startPeriod = 30
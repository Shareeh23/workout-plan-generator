[build]
  builder = "nixpacks"
  buildCommand = "npm install"
  startCommand = "npm start"

[deploy]
  startCommand = "npm start"

[variables]
  NODE_ENV = "production"
  NODE_VERSION = "18.x"

[healthcheck]
  enabled = true
  interval = 30
  retries = 3
  timeout = 10
  startPeriod = 30
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
startCommand = "npm run preview"  # or "npm start" for Create React App
outputDir = "dist"  # or "build" for Create React App

[deploy]
startCommand = "npm run preview"  # or "npm start" for Create React App

[environment]
NODE_ENV = "production"
VITE_API_URL = "fearless-enthusiasm-production-4435.up.railway.app" 